/*
 * The set of elements which are in S1, in S2, or in both S1 and S2.
 * S1 U S2 = { x : x E S1 | x E S2 }
 * Sets cannot have duplicate elements.
 * Multiple occurrences of identical elements have no effect on the cardinality of a set.
 * Properties:
 *   Associative property: S1 U ( S2 u S3 ) = ( S1 U S2 ) U S3
 *   Commutative property: S1 U S2 = S2 U S1
 *   Existence of an identity element (empty set): S1 U ø = S1 
 *   
 */
unsigned long long int unionOfTwoSets(unsigned long long int S1, unsigned long long int S2){
  return S1 | S2;
}
