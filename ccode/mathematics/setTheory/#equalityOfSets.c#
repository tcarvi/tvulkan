/*
 * Equality of Sets
 * Two sets are equal if and only if they are characterized by their elements
 */
_Bool setIsEqualToSet(unsigned long long int S1, unsigned long long int S2){
  return S1 == S2;
}
